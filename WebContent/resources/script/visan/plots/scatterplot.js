function scatterPlot(canvas, visan) {
	
};

scatterPlot.renderPlotCreateOptions = function(optionsContainer) {
	optionsContainer.append($("<p />").text("Please select the X axis:")).append($("<select />").addClass("axis"));
	optionsContainer.append($("<p />").text("Please select the Y axis:")).append($("<select />").addClass("axis"));
	// axis x
	// axis y
};
